Tests for resource usage scopes.

TODO: Also need tests that in dispatches, only bind groups used by the current pipeline at dispatch
time count toward validation. (but in render passes, all bind groups count)
